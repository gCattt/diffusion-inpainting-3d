mesh_dir: data/models
textures_original_dir: data/textures/original
textures_resized_dir: data/textures/resized
textures_corrupted_dir: data/textures/corrupted
renders_dir: data/renders
inpainted_views_dir : data/outputs/inpainted_views
reconstructed_textures_dir: data/outputs/reconstructed_textures

extensions: [jpg, jpeg, png]
resize:
  size: 512
  quality: 95

corruption:
  num_shapes: 4
  line_thickness_min: 3
  line_thickness_max: 12
  min_shape_size: 8
  shape_max_ratio: 0.12
  line_max_ratio: 0.25